setup:
    addons:
        - plan: heroku-postgresql
        - plan: heroku-redis
        - plan: sentry
build:
    docker:
        web: backend/Dockerfile
run:
    web: npm --prefix /opt/server start
    worker:
        command: npm --prefix /opt/worker start
        image: web
