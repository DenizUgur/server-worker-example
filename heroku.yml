setup:
  addons:
    - plan: heroku-postgresql
    - plan: heroku-redis
    - plan: sentry
build:
  docker:
    web: backend/server/Dockerfile
    worker: backend/worker/Dockerfile
run:
  web: npm --prefix /opt/server start
  worker: npm --prefix /opt/worker start