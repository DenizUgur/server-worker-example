{
  "name": "worker",
  "version": "1.0.0",
  "scripts": {
    "start": "node build/index.js",
    "start:dev": "npm run build && node -r dotenv/config build/index.js",
    "dev": "nodemon",
    "build": "rimraf ./build && tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bull": "^3.20.1",
    "throng": "^5.0.0"
  },
  "devDependencies": {
    "@types/bull": "^3.15.0",
    "@types/throng": "^5.0.0",
    "dotenv": "^8.2.0"
  }
}
